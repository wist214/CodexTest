# dotnet-hello

A minimal .NET console app that prints "Hello, world!".

How to run

1. Ensure .NET SDK is installed (recommended: .NET 8.0 or later).
2. From this folder, run:

```bash
dotnet run
```

If `dotnet` is not installed in your environment, install it from https://dotnet.microsoft.com/.
