# CodexTest

This repository includes a tiny console application written in C# on .NET 6.0.

## Running the app

```bash
dotnet run --project HelloWorldApp
```

Alternatively, if you prefer to work from a Visual Studio solution, open `CodexTest.sln` and run the `HelloWorldApp` project from there.
